/* =========================================
   GLOBAL SCRIPTS (Nav, Footer, UI)
   ========================================= */
document.addEventListener('DOMContentLoaded', () => {
    console.log('Global scripts loaded');
    // Add mobile menu toggle logic here later
});